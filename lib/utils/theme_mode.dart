import 'package:flutter/material.dart';

class Th{
  static bool light = false;
  static var appbarText = light ? Colors.white : Colors.black;
  static var appBar = light ? Colors.black : Colors.white;
  static var bodyBackground = light ? Colors.white : Colors.black;
  static var iconTextColors = light ? Colors.black : Colors.white;
}